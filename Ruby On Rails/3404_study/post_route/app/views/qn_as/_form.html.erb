<%= form_with(model: qna, local: true) do |form| %>
  <% if qna.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qna.errors.count, "error") %> prohibited this qna from being saved:</h2>

      <ul>
      <% qna.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :qna_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :qna_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
