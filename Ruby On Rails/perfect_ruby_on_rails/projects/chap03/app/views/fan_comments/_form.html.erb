<%= form_for(fan_comment) do |f| %>
  <% if fan_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fan_comment.errors.count, "error") %> prohibited this fan_comment from being saved:</h2>

      <ul>
      <% fan_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :author_no %>
    <%= f.number_field :author_no %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="field">
    <%= f.label :deleted %>
    <%= f.check_box :deleted %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
